# ET-1 HANDOFF · NEW WINDOW · 2026-08-25 12:42 EDT / 16:42Z

**Branch** `campaign/metered-compute-gate-2026-07-23` · **HEAD `e504348`**
**origin 0/0 · tracked-dirty 0 · CI green on every pushed commit**

**Read first:** `snapshots/ET1_restore_point_2026-08-24_2046EDT.md` — it supersedes
the 1755EDT point, whose §5 still carries a claim corrected at 01:48Z.

---

## 1 · STATE

One commit since the last restore point, shipped and deployed:

```
e504348  web(cockpit): .cockpit-page styling + a guard for the shell-class collision
```

**Deployed and verified live.** Bundle `index-2UnWFJ8x.js` / `index-CZMQAMLu.css`.
52 route objects refreshed, CDN invalidated, `verified 52 routes, 0 problem(s)`.
All deep links serve the new bundle; `/nonexistent` still 404s with the shell body.

`.cockpit-page{display:flex;flex-direction:column;gap:var(--gap-lg);min-width:0}`
confirmed in the served CSS.

**Ownership:** CT-1 assigned ET-1 the whole repo (was: `clarity.pro-mediations.com`).

---

## 2 · INTERRUPTED MID-TASK — pick up here

**Tarball recon. 2 of 4 greps run. STOPPED by CT-1; a revision is coming.**

```
C:\clarityos_code.tar.gz     592,842,437 bytes   2026-04-27 15:48:22
C:\ClarityOS_Code\tarlist.txt  43,351 entries · 5,108,773 bytes   ← WRITTEN, for COW-1
```

**NOTHING EXTRACTED. Nothing moved, nothing committed.**

```
GREP 1 markoff                  RUN — see §3
GREP 2 Clarity_Loop|Langbridg|QC_Post   RUN — see §3
GREP 3 ELINS_LOCAL|WARNO|DIRECTION      ★ NOT RUN
GREP 4 OS_Modules under Archive/?       ★ NOT RUN — this is the one that
        answers whether archiving happened AT import or before it
```

★ Grep 4 is the highest-value unrun item. If `OS_Modules` appears outside
`Archive/` in the 4/27 tarball, the archiving happened during the 5/21 import
and that IS the retirement decision Task A could not find — recorded as a path
difference instead of a commit message.

---

## 3 · TWO CORRECTIONS TO MY OWN CLASSIFICATION

The tarball hunt found **nothing missing from the tree**. But the search
surfaced two files already present that my Task C return got wrong.

**A · Langbridg is NOT spec-only. I classified it wrong.**
```
IN THE WORKING TREE NOW:
  Index_Records/06_GALILEO/Galileo_Service/engines/langbridg.py
```
My Task C enumerated only `Archive/OS_Modules/Langbridg/` (2 .txt) and
concluded SPEC-ONLY. An implementation exists in the GALILEO service tree.
**Langbridg moves SPEC-ONLY → DORMANT.** Unread; I have not opened it.

**B · There are THREE `markoff_core.py`, not one.**
```
Clarity_OS_Operating_System/Archive/OS_Modules/Engine/markoff_core.py
Clarity_OS_Operating_System/engine/markoff_core.py
_scratch_archive_engine/markoff_core.py
```
K3's collision finding — `engine_runtime.py` calls `MarkoffModel` with a
sentence_transformer, `classify()` and Sun Tzu states; `markoff_core.py` has
none — **did not say which of the three it read.** The richer implementation
may already be in the tree at a different path.

★ **NEXT ACTION, and it needs no tarball:** diff the three. If
`Clarity_OS_Operating_System/engine/markoff_core.py` satisfies
`engine_runtime.py`'s signature, the "missing file" was never missing and the
whole tarball line of inquiry closes. **One read. Do this before grep 3 or 4.**

**Clarity_Loop and QC_Post remain .txt-only** in both tree and tarball —
that half of my classification holds.

---

## 4 · RETURNED, AWAITING A RULING

**QC_Post shadow validator.** Recon complete, build NOT started.

```
spec read verbatim: QC_Post.txt v1.0, 2168 bytes
★ the spec contradicts itself — PRIMARY TASKS declares six, the SEQUENCE
  implements five. Task 6 "Drift Removal" has NO STEP. It appears only in
  FAILURE MODES and RECOVERY.

step 1 Receive bullets    BUILDABLE  (sub-check "multi-center artifacts" UNDEFINED)
step 2 Vector alignment   0 of 7 computable — QC_Vectors.txt describes roles in
                          prose, no formula/threshold/unit. Pressure and Basin do
                          NOT map to identity_set.py / effort_threshold.py: those
                          take numbers, the spec's vectors are text properties.
                          Same word, different object.
step 3 ELINS re-check     0 of 4 — ELINS_LOCAL/WARNO/DIRECTION have zero hits.
                          analyze_text returns el_score/ins_score/
                          ratio_classification/reasoning_mode/regression_chain.
                          A keyword-density scorer, not the spec's four modes.
step 4 Markov stability   UNAVAILABLE  (see §3B — may be recoverable after the diff)
step 5 Tone verification  NEW BUILD
step 6 Structural integrity UNAVAILABLE — SIL = "Single Interpretive Line",
                          defined at Clarity_Loop_Controller.txt:10. Not undefined
                          — never PRODUCED, because Clarity_Loop is spec-only.
```

**MY RECOMMENDATION, unruled:** build it as `bullet_contract.py`, not `QC_Post`.
One of six checks is computable. Shipping that under the spec's name encodes the
spec's authority into something covering a sixth of its contract — which is the
laundering the order's own design constraint warns against, at the API level.
Step 1 alone still yields the week of data the order was commissioned for.

---

## 5 · API-BANK ORDER — recon done, build blocked on three answers

Measured, **five source lists and a hardcoded four-tuple**, not three:

```
_PROVIDER_HANDLERS      8   anthropic deepseek gemini local mistral ollama openai xai
_PROVIDER_ENV_KEYS      8   identical
get_model_status        8   identical  (fixed 4b0e8d7)
MODEL_REGISTRY          8   same count, "google" not "gemini"
PROVIDERS_ORDER         5
PROVIDER_CALL_TIMEOUTS  5   ← separate list, backs /config
/health                 4   HARDCODED at runtime_http.py:755-759, includes
                            synthetic "mock"
```

Corrections to the order's table: `/health` does **not** read `PROVIDERS_ORDER`
(hardcoded four incl. `mock`); `/config` does **not** either (reads
`PROVIDER_CALL_TIMEOUTS`); and **`/engines` is not a bank surface at all** —
`EngineId = markov|galileo|library|tizzy`, ClarityOS subsystems, not providers.

**Three answers needed before building:**
1. `mock` row in `/health` — keep as explicit synthetic, or drop?
2. `PROVIDER_CALL_TIMEOUTS` — derive key set with declared-default fallback, or
   leave `/config` narrow deliberately? (Widening means inventing timeouts.)
3. Confirm `/engines` out of scope.

Handlers are 3 routes away from being canonical: `/health`, `/models`, `/config`.

---

## 6 · OPEN, CARRIED

1. **R-4 / R-5** — Stripe webhook registration + what
   `price_1TYDrfGgUU05AeM681Ybd7hU` costs. Gate promotion. `BILLING_MODE=stripe`,
   `STRIPE_MODE=live`, 3/500 seats.
2. **`/enter/` still 404s.** A2 blocked across six envelopes on
   `list_connected_browsers → []`. Rollback value: `https://pro-mediations.com/enter/`.
   ★ `web/src/routes/Success.tsx:36` — the post-Stripe CTA points at it.
3. **#66 is the cheapest ticket open.** BOTH halves exist —
   `api.ts:673 membershipCancel` and `app.py:9883 @app.post("/membership/cancel")`
   (session-gated, not founder-gated). Needs a button. No backend work.
4. **#65** — `/plans` is `INACTIVE_NEXT_PATH` (auth_magiclink.py:88), correct for
   `active == False`. Session handoff works (fragment + HttpOnly cookie,
   `main.tsx:33`). Likely resolved-by-`1a10272`; needs one test purchase to confirm.
   **Days, not weeks.** Ten of eleven hops measured working.
5. **google → gemini rename** — HELD. 4 files, 5 edits, net is live now.
6. **Web suite outside CI.** 92 vitest files / 653 tests gated by nothing;
   `ci.yml` is pytest-only. Includes the guard shipped in `e504348`.
7. **Gate inversion** — `_FILE_MARKERS` opt-in, 33/231 covered.
8. `_audit/provider_connectors.patch` still public; `0c3726d` says removed.
9. Source maps + 5 unreferenced bundle generations public in the bucket.
10. Canon `920` HELD · `540`/`550` title collision · no `400_` index file.
11. Cockpit: **duplicate banner explained** — `Cockpit.tsx:110` and `:117` are two
    distinct `ErrorBanner`s (snapshot vs mesh) rendering together. Not a double
    render; a distinguishability bug. 7 console errors uninvestigated.

---

## 7 · METHOD NOTES THAT KEEP PAYING

- **State a falsifiable prior before the read.** Several were falsified today and
  each falsification was worth more than the confirmations — `remove_member` at
  `:14125`, `SIL` being defined, `runtime_providers.py:225` persisting a bare key.
- **Verify a guard by breaking the thing.** The first `.cockpit` guard PASSED with
  the defect reintroduced; it rendered `<Layout />` alone with an empty Outlet.
  Rewritten and re-verified in three phases. **A guard verified before an edit is
  stale after it.**
- **Baseline before, compare after.** 13 red on the gate turned out pre-existing
  only because the baseline was taken first.
- **The instrument catalogue is ~13 instances, 3 mine.** One shape: a tool that
  matches a naming convention against data that never had to obey it.
