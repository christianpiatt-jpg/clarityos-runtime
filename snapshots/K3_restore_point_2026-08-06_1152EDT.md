# K3 Restore Point — 2026-08-06 11:52 EDT

**Pin:** `be1fb4a` · branch `campaign/metered-compute-gate-2026-07-23` · origin parity as of morning rehydrate
**Tree:** no tracked modifications. Untracked: `command_structure/` (entire directory, incl. the adopted contract), `snapshots/` (this file + `K3_handoff_2026-08-05_1316EDT.md`), ~15 pre-existing ruling-queue paths (June–July ops artifacts).
**Contract in force:** COMMUNICATIONS_CONTRACT v1.8 (adopted today, CT-1). All returns carry §3 authority tags + §14 grammar (MMR-W + MMR-R, MMR generated last, VERIFY separate, IDENTITY withheld).

## Closed today

1. **VERIFY dispatch (OPORD 09:58EDT)** — Signal-Origin Invariant substrate, V-1..V-8 all executed, fresh K3 reads. COW-1 characterization **confirmed on all substance; two phrasing deviations** (V-4: `ctx` default lives at `extract_pattern` `ingestion_engine.py:204-206`, not on `_check_whiplash` `:184`; call at `:238` unconditional absent hard override — opt-in is behavioral, not a call guard; `docs/feedback_layer.md:342` imprecise [T-STRUCTURAL]). V-8: `_layer_8_qc_self` (`standard_elins.py:334-349`) = validator_overlap 1.0, a check that can't fail. Three orphans confirmed orphans.

2. **Primitive cross-set recon** — 9 sets enumerated (5 live code, 4 design). Common core: **pressure is the only universal**; six-tuple duplicated (`standard_elins.py:62` = `elins_v2_view.py:55`); only trust+alignment bridge code→kernel enums; only pressure+drift bridge code→SystemState; **margin code-absent everywhere**. Tags-that-alter-readings register by mechanism: gate (domain tag, `_HIGH_PRESSURE` ×4 files, env flag) · quantization (regime→{0.9,0.5,0.2}, collapse→{0,0.5,1.0}, basin→{0,0.5,1.0}) · sign (STRESS/RELIEF partition — negation-blindness hazard) · abstention (`unclear` — semantic exception at `signal_clarity`, `intelligence_kernel.py:1719-1720`) · phantom (`top_primitive="alignment"` alphabetical tiebreak). Collision register: pressure ×5 vocabularies, tension, trust, alignment, drift.

3. **Motion/Position Theorem v1.0 — ACCEPTED** as governing doctrine. Substrate confirmations: ten layers pure f(text) (V-1/V-2); Lane 1 `delta.intensity_mean` null-at-n=1 = theorem already implemented (`ef0c19d`). Consequence register: Layer 5 `signal` + Layer 6 forecast (`standard_elins.py:270-289`) = motion tags from one position, reclassified undefined. `frame_boundary`/`origin` enum = spec-side, zero code referents. `regression_trace.py:53` already carries origin-claim detection lexicon.

4. **Comms Contract v1.8 — ADOPTED** to `command_structure/COMMUNICATIONS_CONTRACT_v1.8.md`. Three mechanical changes (Status → ADOPTED CT-1 2026-08-06; in-force line; closed the draft's unclosed code fence), zero content changes. §17.2 verified (four `_HIGH_PRESSURE` definitions: `ingestion_engine.py:56`, `emotional_alignment_engine.py:60`, `primitive_selection_engine.py:55`, `azimuth_transition.py:357`); §17.3 verified fresh (`standard_elins.py:321-322` alphabetical tiebreak, `:241` dominant ternary). §14.3's 66-MMR extraction claim accepted from COW-1, unverified (transcript not held).

5. **REFRAME evaluation (qc_pressure) — FEASIBLE, three moves.** Decisive body read: `compute_curvature` (`dewey_pipeline.py:456-489`) = `sim_mean − sim_primary`, a **signed Δ of cosine similarities — not a bend**. Frame error = **`abs()` at the consumers**, not "averaging bends." **Three computation sites, not two:** `app.py:8314-8315`, `app.py:8685-8688`, `dewey_pipeline.py:681-682`. Pathway: additive `qc_pressure_grad` (signed, null-on-empty per §17.3), one shared helper (kills §17.2 triplication), `qc_pressure` untouched (persisted in `markov_states_store.py:11`; feeds `pressure_score` :728, `pressure_trend` :916). Move 2: `geodesic_distance` (`:273`) has **zero callers** — rename or delete, one line + comment sweep (`:26`, `app.py:2800`). Move 3: curvature tri-referent CCIR-C — (a) signed Δ, (b) mean-abs ×3, (c) boolean token flag (`azimuth_transition.py:844`, inside an all-geometry-named `EmotionalGeometry`).

## Open / pen-held

- **Commit gate** — adopted contract + both snapshots. `command_structure/` is entirely untracked: durability exposure, same class as the old `source_docs/` gap. Scoped adds, signed per §10.
- **CT-1 gate (§17.1)** — move 1 `qc_pressure_grad` FRAGO; K3 offered to draft on pen's word.
- **CT-2** — three CCIR-C filings ready, all carrying [T-SUBSTRATE]: §17.2 (four `_HIGH_PRESSURE` defs) · §17.3 (alphabetical `alignment` default on null) · curvature tri-referent.
- **CT-1 rulings** — `frame_boundary` vs the undetermined/never-origin fork (v1.8 adoption block) · **flow vs force** (gates strain score + R-6; today's REFRAME evaluation is evidence for FLOW-primary).
- **Standing queue** — loosened lint-conditions spec · R6 probe (pen's 11-word text) · census extension (`forecast_engine.py` + `regional_elins.py`) · `.get("intensity_mean", 0.0)` residual lane assignment · sidebar error-read (ET-1) · Mistral routing check · uncommitted 1316 snapshot (now superseded by this file; still worth committing for the record).
- Fixture `soldierslawyer@gmail.com` untouched all day.

## Restore procedure (new window)

1. Rehydrate: `git status` + `git log --oneline -1` — expect `be1fb4a` unless pen committed; check parity with origin.
2. This file is state. Contract v1.8 governs all returns.
3. Next three action items — see below; none start without pen's word.

## Next three action items

1. **Commit gate (durability)** — contract + snapshots, scoped adds, signed per §10. Everything closed today is tree-only until this lands.
2. **Move-1 FRAGO draft** — `qc_pressure_grad`: additive signed field, shared helper, null-on-empty, three sites. Sized like Lane 1.
3. **Three CCIR-C filings to CT-2** — §17.2, §17.3, curvature tri-referent. Evidence packages already carry [T-SUBSTRATE]; filing is clerical.
