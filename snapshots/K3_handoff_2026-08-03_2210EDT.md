# K3 restore point — 2026-08-03 ~22:10 EDT

Lane: Kimi K3, witness — read-only, advisory. Pen holds all gates. Workspace `C:\ClarityOS_Code`. Integrates `K3_handoff_2026-08-03.md` (12:56), `_1424.md` (14:24), `_1655EDT.md` (16:55). This snapshot closes with a missing-middle reflection per comms-clause v0.3.

## Banked since 16:55

### 1. Softmax WIRE trace — verified + banked
`analysis/trace_state_distribution_reconciliation_2026-08-03.md`. COW-1 derivation verified empirically: null panel byte-exact (P0=.3333, P3=.2222, P6=.4444, collapse "soft"); measured band **[0.147, 0.465]** (COW-1's [0.175, 0.475] slightly off — shared factors); hard collapse 17/200k random profiles; **trust-inversion: "hard" requires saturated trust + zero alignment + max pressure**; null → timescale near=1.0 (maximally urgent AND maximally ruptured). New contract row: **threshold calibration must cite the achievable band of the variable it gates.**

### 2. Nine-file Copilot batch — evaluated (files in `source_docs/copilot_tools_2026-08-03/`)
- **PATTERN EVALUATION PROTOCOL v1.0 — ADOPT** as evaluation layer. κ gate = decorrelation measure; pre-registered Brier skill vs baseline = no-oracle accumulation with a score; evidence typing (OBSERVED/CLAIMED/INFERRED/CONTRADICTED/MISSING) = citation classes; failure #1 = our band probe. Worked ref: Piatt v. Collins, predictions locked 2026-07-15, resolution 2026-09-30, **freeze in effect**.
- confucian_loop — runnable toy; keep sensitivity-sweep method, not model. Universal diagnostic algorithm — mine evergreen-artifact shape (persistence × spread × impact) for archive-poisoning detection. UPGT + Dynamic Pressure Map — **do not adopt** (types without estimation rules; pressure map defaults base 5.0 "Neutral" on lexicon miss = our null disease). Gohard — EWS features (variance↑, recovery↑, autocorrelation↑) computable on envelope time series; hysteresis indicts forecast's symmetric 12%/day reversion.
- Three ops-geometry files: "Christian — yes" = **the anti-contract** (no null form, unwitnessed green predicates, mandatory motion); triangles = diagram literalism, Triangle 3/10 real but souvenir of code-level enforcement. Pile tally: 1 instrument, 1 toy, 2 mined ideas, 5 dialect artifacts.

### 3. Recon 2+3 (HQ dispatch, K3 portion)
- **Config surface:** dispatch registry = `TASK_DEFAULTS` (12 tasks) + 8 providers + 3-level lane override (meta["model"] / preferred_model / founder default). **Mistral wired as provider** (model_router:914/:958) — decorrelation pilot needs harness, not levers. Mock-default flags at pin: `CLARITYOS_BACKEND=memory`, `BILLING_MODE=mock` + auto-confirm, `RATE_LIMIT_ENFORCE=0`, felt-gap `=0`, email `log`. **v80.1 markov note documents a real silent-mock-in-prod incident** (ollama unkeyed → mock recast shipped; fixed by default flip; `CLARITYOS_ANTHROPIC_KEY` confirmed set on live revision 00092-tep via comment).
- **Docs register (5/5 read):** emotional_physics.md line anchors stale ~140–200 lines + `claude-3.7` id stale; docs canonize no-per-layer-validation, mock-parsed-normally, skills_export 5-key vs inline 4-key (two EP contracts in repo). elins_deep_spec accurate but **stops at v34 — the v53 adapter (where the WIRE defect lives) has no intent doc**. geometry_system.md: "no geometric-primitive system" — collides with Copilot geometry claims. cognitive_pipeline.md: five-layer pipeline was design canon, no implementation. **ERA spec (2026-05-11) = contract precedent: `ReframeType.NONE`, never-escalate cap, break-smaller — null forms already design canon in one module.**

### 4. Corrections register (Claude/ET-1 return)
- Serving inversion corrected: **`00122-sir` @ 100%, `00119-gas` rollback armed** — K3 12:56 record agreed throughout.
- **B-2 Step 1 WAS executed:** `00124-yib` on `b2-verify` carries `FELT_GAP_READER_ENABLED=1` + allowlist at 0% traffic. arc_records zero = **no traffic on flag-bearing revision, not flag-never-set. B-2 needs no deploy.**
- **Empty-chair allowlist:** scoped to account with one vault doc, no conversations; arc record requires completable pair (prior assistant reply + user follow-up, `:1112`). Gate proof (1 key first_user / 0 admin) testable at 0% on tag, zero prod risk. Gate proof ≠ accumulation — separate rulings.
- **Dispatch 1B cancelled:** `0sfPXLQR` minted post-fix, named reserve in K3 12:56 (:50); "unused = orphan" falsified. Docstring trap (arc_records = key prefix, `:818`/`:821`) confirmed by two lanes independently — witness-overlap, banked.
- **Freeze ruling input:** felt-gap enable = sensor not parameter → freeze-legal; caveats: vault state act; freeze-compatible B-2 = gate proof at 0% only.

### 5. meta["model"] P/D/A verdict (provenance honesty)
Router = actual-source honest (`mock:True` + `fallback_error`, `:494`/`:497`). Consumers split: **thread chat = attempted-provider** — mock dropped at seam, honesty survives only as `[mock …]` content prefix (`kernel:1029`, `:1223`); **EP = attempted-provider + worse** — mock text fails JSON parse → disguised as `parse_error`, wrong-cause signal (`:1750-1775`); markov + `/model/complete` + ESO = actual-source (markov raises 502 on mock+fallback_error, `app.py:950-957`). Default mismatch: `app.py:963` (True) vs `:11583` (False). Feeds contract row: **source provenance is itself a citation field, dropped at the kernel seam on both model-path surfaces.**

### 6. Comms contract clause → v0.3
`source_docs/ClarityOS_CommsContract_COP_Constraint_Geometry_Clause.md` — new section **"The missing-middle close"**: standard move on closing a return (name verified near end → name every extension past it → mark NODE-vs-WIRE no-traceable-effect claims). Failure named: verified-prefix carry. Advisory draft; CT-2 fold-in untraced.

## Operational state
- B-2: gate proof at 0% on `b2-verify` is pen's next act (two-turn rule). No deploy needed.
- Standing register otherwise unchanged from 12:56/14:24 (provider-health untrusted, threads session-only, 13-red baseline, login throttle, `_bootstrap_admin` defect, invite reserve `0sfPXLQR` INTACT — Dispatch 1B cancelled).

## Open items (priority order)
1. Pen rulings: B-2 gate proof execution; allowlist re-scope (sampling amendment, separate ruling); v0.3 spec amendment disposition (carried).
2. Build order (pen's decision): Pass 6 noise floor + deterministic strip-checker → decorrelation pilot (κ on 08-01 control + admin corpus; Mistral harness) → orchestrator. NEW: mock-provenance propagation fix (thread + EP seams) belongs in build 1 — it's a citation-field drop, one-line-class.
3. Six-test gate-review file (offered, not fired; now includes band assertions + trust-inversion regression + mock-provenance tests).
4. Test 6 return + seed still not in workspace — residue audit pending pen's drop.
5. Protocol adoption pilot: Stage 0–1 on ELINS itself (offered, not fired).

## Missing-middle close (per clause v0.3)
- **Verified near end:** every file:line cited in §1–§6 was read this session at pin; the null-path numbers were executed, not derived; the trace doc, batch copies, and clause v0.3 exist on disk.
- **Extensions past it:** runtime claims (flag carriage on `00124-yib`, allowlist scope, serving split) are ET-1/pen-attested, not K3-verified — registered as coherent-with-record, not Corr'd. The build-order recommendation is judgment, not measurement; decorrelation remains UNMEASURED until the κ pilot runs.
- **No traceable effect:** the comms-clause v0.3 "standard move" is text; nothing enforces it. The Protocol "adoption" is a recommendation; nothing wires it. Both are NODE until pen rules.
