import ClarityOSSurface from "./components/ClarityOSSurface/ClarityOSSurface";

// Surface v1 harness — thin wrapper. No router, no auth, no API.
export default function App() {
  return <ClarityOSSurface />;
}
