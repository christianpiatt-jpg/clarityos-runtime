# ClarityOS Runtime — development + test dependencies.
#
# These are the libraries the test suite uses. They are NOT shipped
# with the production container; the production image installs
# requirements.txt only.
#
# Versions match what the CI workflow installs
# (.github/workflows/ci.yml, deploy.yml, release.yml).

# Production deps come from requirements.txt — pip will install them
# transitively when this file is processed via ``pip install -r``.
-r requirements.txt

# --- Test runner -----------------------------------------------------------
pytest==8.3.3
pytest-cov==5.0.0

# --- HTTP test client used by tests/conftest.py::AppClient -----------------
httpx==0.27.2
